{
  "$schema": "../gen/schemas/desktop-capability.schema.json",
  "identifier": "default",
  "description": "default capability",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
